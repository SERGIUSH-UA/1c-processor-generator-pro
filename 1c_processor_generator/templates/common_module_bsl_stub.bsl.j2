{% if module_name == "УправлениеПечатью" %}
// Stub module for УправлениеПечатью (print management)
// Generated for EPF validation purposes

// Проверяет, нужно ли печатать указанный макет.
Функция НужноПечататьМакет(КоллекцияПечатныхФорм, ИмяМакета) Экспорт
	Возврат Истина;
КонецФункции

// Выводит табличный документ в коллекцию печатных форм.
Процедура ВывестиТабличныйДокументВКоллекцию(КоллекцияПечатныхФорм, ИмяМакета, Представление, ТабличныйДокумент, ОбъектыПечати = Неопределено, ИмяФайла = "") Экспорт
	// Stub
КонецПроцедуры

// Задает область печати документа.
Процедура ЗадатьОбластьПечатиДокумента(ТабличныйДокумент, НачалоОбласти, ОбъектыПечати, Ссылка) Экспорт
	// Stub
КонецПроцедуры

// Возвращает сведения о печатной форме из коллекции.
Функция СведенияОПечатнойФорме(КоллекцияПечатныхФорм, ИмяМакета) Экспорт
	Возврат Неопределено;
КонецФункции

// Возвращает макет печатной формы.
Функция МакетПечатнойФормы(ПутьКМакету) Экспорт
	Возврат Неопределено;
КонецФункции
{% else %}
// Stub module for {{ module_name }}
// Generated for EPF validation purposes
{% endif %}

//
// BSL код для validator.epf - Form handlers
// Перевірка синтаксису зовнішніх обробок (EPF файлів)
//

// ============================================================================
// ОБРАБОТЧИКИ ФОРМЫ
// ============================================================================

&НаСервере
Процедура ПриСозданииНаСервере(Отказ, СтандартнаяОбработка)
    // Ініціалізація при відкритті форми
КонецПроцедуры

&НаКлиенте
Процедура ПроверитьСинтаксис(Команда)
    // Перевірка з інтерфейсу
    Если ПустаяСтрока(Объект.ФайлEPF) Тогда
        Сообщить("Не указан файл EPF для проверки");
        Возврат;
    КонецЕсли;

    ПроверитьСинтаксисНаСервере(Объект.ФайлEPF);
КонецПроцедуры

&НаСервере
Процедура ПроверитьСинтаксисНаСервере(ПутьКФайлу)
    // Викликаємо процедуру з ObjectModule (де вона визначена)
    РеквизитФормыВЗначение("Объект").ПроверитьСинтаксисEPFПрограммно(ПутьКФайлу);
КонецПроцедуры

// Процедурні тести для калькулятора (складна логіка)
//
// ВАЖЛИВО: Ці тести інжектуються в ObjectModule і виконуються через External Connection.
// Використовуйте ObjectModule style:
// - Прямий доступ до атрибутів: Число1, Число2, Результат (БЕЗ Объект.)
// - Прямі виклики процедур: Добавить(), Вычесть() (БЕЗ НаСервере suffix)
// - БЕЗ директив компіляції: &НаСервере, &НаКлиенте (тільки для форм)

Процедура Тест_ПоследовательныеОперации() Экспорт
	// Тест: послідовне виконання операцій

	// 10 + 5 = 15
	Число1 = 10;
	Число2 = 5;
	Добавить();

	Если Результат <> 15 Тогда
		ВызватьИсключение "Помилка додавання: очікувалось 15, отримано " + Результат;
	КонецЕсли;

	// 15 - 5 = 10
	Число1 = Результат;
	Число2 = 5;
	Вычесть();

	Если Результат <> 10 Тогда
		ВызватьИсключение "Помилка віднімання: очікувалось 10, отримано " + Результат;
	КонецЕсли;

	// 10 * 3 = 30
	Число1 = Результат;
	Число2 = 3;
	Умножить();

	Если Результат <> 30 Тогда
		ВызватьИсключение "Помилка множення: очікувалось 30, отримано " + Результат;
	КонецЕсли;

КонецПроцедуры

Процедура Тест_БольшиеЧисла() Экспорт
	// Тест: робота з великими числами

	Число1 = 999999999;
	Число2 = 1;
	Добавить();

	Если Результат <> 1000000000 Тогда
		ВызватьИсключение "Помилка з великими числами: очікувалось 1000000000, отримано " + Результат;
	КонецЕсли;

КонецПроцедуры

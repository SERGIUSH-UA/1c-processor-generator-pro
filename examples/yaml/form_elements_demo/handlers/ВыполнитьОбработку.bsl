// Обработчик команды "Выполнить"

// Валидация
Если ПустаяСтрока(Объект.ТипОперации) Тогда
	Сообщить("Выберите тип операции!");
	Возврат;
КонецЕсли;

Если ПустаяСтрока(Объект.РежимРаботы) Тогда
	Сообщить("Выберите режим работы!");
	Возврат;
КонецЕсли;

// Выполнение обработки
Результат = ВыполнитьОбработкуНаСервере();

Если Результат.Успех Тогда
	Объект.Статус = "✓ Обработка выполнена успешно";
	ПоказатьПредупреждение(, Результат.Сообщение);
Иначе
	Объект.Статус = "✗ Ошибка: " + Результат.Сообщение;
	ПоказатьПредупреждение(, "Ошибка: " + Результат.Сообщение);
КонецЕсли;

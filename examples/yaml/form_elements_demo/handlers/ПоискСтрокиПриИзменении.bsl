// Обработчик изменения поисковой строки (InputField с InputHint)

Если ПустаяСтрока(Объект.ПоискСтроки) Тогда
	Объект.Статус = "Поле поиска очищено";
Иначе
	Объект.Статус = "Поиск: """ + Объект.ПоискСтроки + """";
КонецЕсли;

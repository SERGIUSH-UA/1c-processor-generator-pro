// Обробник команди УстановитьРоль
// Установка ролі вибраному користувачу

// Перевіряємо заповнення полів
Если НЕ ЗначениеЗаполнено(Объект.Пользователь) Тогда
	Сообщить("Выберите пользователя!");
	Возврат;
КонецЕсли;

Если НЕ ЗначениеЗаполнено(Объект.Роль) Тогда
	Сообщить("Укажите имя роли!");
	Возврат;
КонецЕсли;

// Викликаємо серверну процедуру
УстановитьРольНаСервере();
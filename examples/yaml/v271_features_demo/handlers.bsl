// v2.71.x Features Demo - Handlers
// Демонстрация всех новых возможностей v2.71.0 - v2.71.3

#Область ОбработчикиКоманд

// OK button handler (default_button + shape_representation: Always)
&НаКлиенте
Процедура ВыполнитьОК(Команда)

	// Проверка обязательного поля (auto_mark_incomplete)
	Если ПустаяСтрока(Объект.Наименование) Тогда
		Сообщить("Заполните наименование!");
		Возврат;
	КонецЕсли;

	// Демонстрация mark_negatives
	Если Объект.Сумма < 0 Тогда
		Сообщить("Внимание: сумма отрицательная! (подсвечена красным)");
	КонецЕсли;

	Сообщить("OK выполнено успешно!");

КонецПроцедуры

// Cancel button handler (shape_representation: WhenActive)
&НаКлиенте
Процедура Отмена(Команда)

	Закрыть();

КонецПроцедуры

// Flat button handler (shape_representation: None)
&НаКлиенте
Процедура ПлоскоеДействие(Команда)

	Сообщить("Это плоская кнопка без рамки (shape_representation: None)");

КонецПроцедуры

#КонецОбласти

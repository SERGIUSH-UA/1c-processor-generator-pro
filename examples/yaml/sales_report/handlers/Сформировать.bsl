// Client-side validation before generating report
// Перевірка параметрів перед формуванням звіту

// Validate start date
Если НЕ ЗначениеЗаполнено(Объект.ДатаНачала) Тогда
	Сообщить("Укажите дату начала периода!");
	Возврат;
КонецЕсли;

// Validate end date
Если НЕ ЗначениеЗаполнено(Объект.ДатаОкончания) Тогда
	Сообщить("Укажите дату окончания периода!");
	Возврат;
КонецЕсли;

// Validate date range logic
Если Объект.ДатаОкончания < Объект.ДатаНачала Тогда
	Сообщить("Дата окончания не может быть меньше даты начала!");
	Возврат;
КонецЕсли;

// Call server-side data loading
СформироватьНаСервере();

Сообщить("Отчет сформирован!");

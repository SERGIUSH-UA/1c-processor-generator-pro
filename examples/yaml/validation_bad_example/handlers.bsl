// ====================================================================
// Обробники для НеправильныйПример
// ====================================================================
// ✅ Синтаксис BSL правильний (Синтаксична перевірка пройде)
// ❌ Є семантичні помилки (Семантична перевірка виявить)
// ====================================================================

// ❌ ПОМИЛКА 1: Пустий обробник (Семантична перевірка виявить через -EmptyHandlers)
&НаКлиенте
Процедура ПриОткрытии(Отказ)
    // Порожній обробник - знижує продуктивність!
КонецПроцедуры

// ❌ ПОМИЛКА 2: Невикористовувана процедура (Семантична перевірка виявить через -UnreferenceProcedures)
// Ця процедура ніде не викликається
&НаСервере
Функция НевикористовуванаФункция()
    Возврат "Ця функція ніде не викликається";
КонецФункции

// ❌ ПОМИЛКА 3: Пустий обробник команди (Семантична перевірка виявить через -EmptyHandlers)
&НаКлиенте
Процедура ТестоваяКоманда(Команда)
    // Порожній обробник команди
КонецПроцедуры

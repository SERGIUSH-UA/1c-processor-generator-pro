// ============================================================
// Обработчики для HTMLDashboard
// Демонстрація HTML/CSS/JS інтерфейсу в 1С як посередника
// ============================================================

// Подія форми: ПриСозданииНаСервере - автоматичне оновлення HTML при відкритті
&НаСервере
Процедура ПриСозданииНаСервере(Отказ, СтандартнаяОбработка)
	// Автоматично завантажуємо HTML при відкритті форми
	ОбновитьДашбордНаСервере();
КонецПроцедуры

// Команда: RefreshDashboard - ручне оновлення HTML
&НаКлиенте
Процедура RefreshDashboard(Команда)
	ОбновитьДашбордНаСервереКлиент();
КонецПроцедуры

// Клієнтський wrapper для серверного виклику
&НаКлиенте
Процедура ОбновитьДашбордНаСервереКлиент()
	ОбновитьДашбордНаСервере();
КонецПроцедуры

// Серверна процедура оновлення HTML
&НаСервере
Процедура ОбновитьДашбордНаСервере()
	// Отримуємо HTML з підставленими placeholders
	DashboardHTML = ПолучитьТекстМакетаDashboard();
КонецПроцедуры

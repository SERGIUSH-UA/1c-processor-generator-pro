#Область ОбработчикиСобытийФормы

&НаКлиенте
Процедура ПриОткрытии(Отказ)

    // Ініціалізація форми
    ТестовыйРеквизит = "Привет, мир!";
    Число = 123.45;

    Сообщить("Форма открыта. Валидация BSL активна!");

КонецПроцедуры

&НаСервере
Процедура ПриОткрытииНаСервере()
	// Вставить содержимое обработчика.
КонецПроцедуры

#КонецОбласти

#Область ОбработчикиСобытийЭлементовШапкиФормы

// Обработчики событий элементов формы

#КонецОбласти

#Область ОбработчикиКомандФормы

&НаКлиенте
Процедура ВыполнитьКоманду(Команда)

    // Викликаємо серверну частину
    ВыполнитьНаСервере();

КонецПроцедуры

#КонецОбласти

#Область СлужебныеПроцедурыИФункции

&НаСервере
Процедура ВыполнитьНаСервере()

    // Перевіряємо валідацію
    Если ПустаяСтрока(ТестовыйРеквизит) Тогда
        Сообщить("⚠️ Тестовый реквизит пустой!");
        Возврат;
    КонецЕсли;

    Если Число <= 0 Тогда
        Сообщить("⚠️ Число должно быть > 0");
        Возврат;
    КонецЕсли;

    // Викликаємо допоміжну функцію
    Результат = ВычислитьРезультат(Число);

    Сообщить("✅ Проверка выполнена успешно! Результат: " + Формат(Результат, "ЧДЦ=2"));

КонецПроцедуры

&НаСервере
Функция ВычислитьРезультат(Значение)

    // Просте обчислення для демонстрації
    Возврат Значение * 2 + 100;

КонецФункции

#КонецОбласти
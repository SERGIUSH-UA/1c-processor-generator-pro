// ============================================================
// Обробник команди "Розрахувати" (Розрахувати сумму)
// Handler for "Calculate" command (Calculate total amount)
// ============================================================

// Обробник виконання команди "Розрахувати" (клієнт)
// Execute handler for "Calculate" command (client)
&НаКлиенте
Процедура РозрахуватиВиконати(Команда)
	РозрахуватиВиконатиНаСервері();
КонецПроцедуры

// Розрахунок суми на сервері
// Calculate total on server
&НаСервере
Процедура РозрахуватиВиконатиНаСервері()
	ОбщаяСума = Объект.Количество * Объект.Цена;

	// Виводимо результат трьома мовами
	// Output result in three languages
	Сообщить("Товар / Product: " + Объект.Товар);
	Сообщить("Количество / Quantity: " + Формат(Объект.Количество, "ЧГ="));
	Сообщить("Цена / Price: " + Формат(Объект.Цена, "ЧДЦ=2; ЧГ="));
	Сообщить("Загальна сума / Total amount: " + Формат(ОбщаяСума, "ЧДЦ=2; ЧГ="));
КонецПроцедуры

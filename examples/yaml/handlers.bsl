// Подія форми: ПриСозданииНаСервере
&НаСервере
Процедура ПриСозданииНаСервере(Отказ, СтандартнаяОбработка)
    // Значення за замовчуванням для демо
    Объект.ФИО = "Иванов Иван Иванович";
    Объект.Должность = "Директор по развитию";
    Объект.Компания = "ООО Рога и Копыта";
    Объект.Телефон = "+7 (999) 123-45-67";
    Объект.Email = "ivanov@company.com";

    // Генеруємо візитку
    ОбновитьВизиткуНаСервере();
КонецПроцедуры

// Команда: Обновить
&НаКлиенте
Процедура Обновить(Команда)
    ОбновитьВизиткуНаСервере();
КонецПроцедуры

// Серверна процедура оновлення візитки
&НаСервере
Процедура ОбновитьВизиткуНаСервере()
    // ПолучитьТекстМакетаCard() автоматично генерується template automation
    CardHTML = ПолучитьТекстМакетаCard();
КонецПроцедуры

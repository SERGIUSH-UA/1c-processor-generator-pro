// Initialize wizard with default values
// Ініціалізація майстра зі значеннями за замовчуванням

// Set default encoding
Если НЕ ЗначениеЗаполнено(Объект.Кодировка) Тогда
	Объект.Кодировка = "UTF-8";
КонецЕсли;

// Set default catalog
Если НЕ ЗначениеЗаполнено(Объект.Справочник) Тогда
	Объект.Справочник = "Номенклатура";
КонецЕсли;

// Enable "Update existing" by default
Объект.ОбновлятьСуществующие = Истина;

// Start on Step 1
Элементы.ШагиМастера.ТекущаяСтраница = Элементы.Шаг1ВыборФайла;

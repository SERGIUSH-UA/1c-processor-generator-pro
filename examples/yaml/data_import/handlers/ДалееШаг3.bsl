// Navigate to Step 3 (Import Settings)
// Перейти до Кроку 3 (Налаштування імпорту)

// Validate preview has data
Если Предпросмотр.Количество() = 0 Тогда
	Сообщить("Нет данных для импорта!");
	Возврат;
КонецЕсли;

Элементы.ШагиМастера.ТекущаяСтраница = Элементы.Шаг3Настройки;

# Приклад тестування форм (v2.23.2+)

Цей приклад демонструє як тестувати форми обробок використовуючи **Test Framework v2.23.2+** з архітектурою ObjectModule/Form тестів.

## ⚠️ КРИТИЧНО: Обмеження тестування форм

**❌ ТЕСТИ ФОРМ НЕ ПРАЦЮЮТЬ в поточній версії (v2.23.2+)**

Після детального дослідження (2025-11-18) з'ясовано, що **тестування форм технічно неможливе** через фундаментальні обмеження COM:

### Що НЕ працює:
- ❌ V83.Application (Automation Server) - недоступний з Python/PowerShell
- ❌ V83.COMConnector.ПолучитьФорму() - помилка "Інтерактивні операції недоступні"
- ❌ Форми через External Connection (headless режим за дизайном)

### Що ПРАЦЮЄ:
- ✅ **Тести ObjectModule** (бізнес-логіка, дані, команди)
- ✅ Декларативні та процедурні тести (ObjectModule-стиль)
- ✅ V83.COMConnector для headless тестування

### Статус цього прикладу:
Цей приклад демонструє **БАЖАНУ архітектуру** для тестування форм, але:
1. ❌ Форми поки що **НЕ ФУНКЦІОНАЛЬНІ**
2. ✅ Приклад збережений для:
   - Документування призначеної архітектури
   - Майбутнього використання якщо обмеження буде вирішено
   - Шаблону для альтернативних підходів

**Технічна документація:** `docs/research/V83_INVESTIGATION_REPORT.md`

**Альтернативи:**
- Ручне тестування через Конфігуратор 1С
- Веб-клієнт + Selenium (для майбутнього)
- ObjectModule тести (працюють ідеально)

---

## Що робить обробка

Проста обробка-калькулятор з:
- **3 поля:** Число1, Число2, Результат
- **Кнопка "Сложить"** - додає два числа
- **Кнопка "Очистить"** - обнуляє всі поля

## Структура файлів

```
table_test_example/
├── config_simple.yaml          # Конфігурація обробки (калькулятор)
├── handlers_simple.bsl          # Обробники кнопок
├── tests/
│   ├── tests.yaml              # ✅ Конфігурація тестів (v2.23.2 формат)
│   └── form_tests.bsl          # ✅ BSL процедури з тестами
└── README.md                    # Цей файл
```

## ⚠️ ВАЖЛИВО: Формат тестів v2.23.2+

Тест фреймворк використовує **НОВИЙ формат** з v2.23.2. Старий формат v2.16.0 **НЕ ПРАЦЮЄ**.

### ❌ Старий формат (v2.16.0 - НЕ ПРАЦЮЄ)

```yaml
tests:  # ← ПОМИЛКА: старий формат
  - name: test_addition
    type: procedural
    connection_type: Automation
    procedure: |
      Форма = ПолучитьФорму(...);  # ← Inline BSL більше НЕ підтримується
```

### ✅ Новий формат (v2.23.2 - ПРАЦЮЄ)

```yaml
forms:  # ← ПРАВИЛЬНО: forms замість tests
  - name: Форма  # ← ОБОВ'ЯЗКОВО: ім'я форми
    procedural:
      file: form_tests.bsl  # ← BSL в окремому файлі
      procedures:
        - Тест_Додавання
```

## Тести

Створено 5 procedural тестів для форми:

### 1. Тест_Додавання
Перевіряє базову арифметику: `5 + 3 = 8`

### 2. Тест_Очищення
Перевіряє що кнопка "Очистить" обнуляє всі поля

### 3. Тест_ВідємніЧисла
Перевіряє роботу з від'ємними числами: `-10 + 5 = -5`

### 4. Тест_ВеликіЧисла
Перевіряє роботу з великими числами: `1000000 + 2000000 = 3000000`

### 5. Тест_Нуль
Перевіряє додавання нуля: `10 + 0 = 10`

## Як запустити

### Крок 1: Згенерувати обробку

```bash
python -m 1c_processor_generator yaml \
  --config examples/yaml/table_test_example/config_simple.yaml \
  --handlers-file examples/yaml/table_test_example/handlers_simple.bsl \
  --output-format epf
```

Це створить EPF файл: `tmp/ТестированиеПростое.epf`

### Крок 2: Запустити тести

```bash
python -m 1c_processor_generator.test_runner \
  --tests-config examples/yaml/table_test_example/tests/tests.yaml \
  --epf-path tmp/ТестированиеПростое.epf \
  --ib-path "%APPDATA%\1C\epf_compiler_cache\persistent_ib" \
  --processor-name ТестированиеПростое \
  --verbose
```

### Крок 3: Очікуваний результат

```
🚀 RUNNING TESTS (v2.23.2 - auto-detection)
================================================================================

📋 Form: Форма (5 procedural tests)

✅ Тест_Додавання - PASSED (0.15s)
   ✓ Тест пройдено: 5 + 3 = 8

✅ Тест_Очищення - PASSED (0.12s)
   ✓ Тест пройдено: всі поля успішно обнулені

✅ Тест_ВідємніЧисла - PASSED (0.11s)
   ✓ Тест пройдено: -10 + 5 = -5

✅ Тест_ВеликіЧисла - PASSED (0.10s)
   ✓ Тест пройдено: 1000000 + 2000000 = 3000000

✅ Тест_Нуль - PASSED (0.09s)
   ✓ Тест пройдено: 10 + 0 = 10

================================================================================
📊 SUMMARY
================================================================================
Total tests: 5
✅ Passed: 5
❌ Failed: 0
⏱️  Execution time: 0.57s

🎉 ALL TESTS PASSED!
```

## Ключові концепції тестування форм (v2.23.2)

### 1. Архітектура: ObjectModule vs Form тести

**ObjectModule тести** (External Connection - швидко, без UI):
```yaml
objectmodule_tests:
  procedural:
    file: objectmodule_tests.bsl
    procedures: [Тест_БізнесЛогіки]
```

**Form тести** (Automation Server - повільно, з UI):
```yaml
forms:
  - name: Форма  # ← ОБОВ'ЯЗКОВО!
    procedural:
      file: form_tests.bsl
      procedures: [Тест_Кнопки]
```

### 2. Стиль BSL коду для Form тестів

**Form Module style** (з Объект., &НаСервере):
```bsl
&НаСервере
Процедура Тест_Додавання() Экспорт
    Объект.Число1 = 5;           // ← З Объект.
    Сложить(Неопределено);        // ← З параметром Команда

    Если Объект.Результат <> 8 Тогда
        ВызватьИсключение "Помилка";
    КонецЕсли;
КонецПроцедуры
```

**ВАЖЛИВО:**
- ✅ `Объект.Число1` (з префіксом)
- ✅ `&НаСервере` (обов'язково)
- ✅ `Сложить(Неопределено)` (параметр Команда)
- ✅ `Экспорт` (обов'язково)

### 3. Чому НЕ ObjectModule тести?

ObjectModule тести **НЕ ПРАЦЮЮТЬ** для тестування форм:
- ❌ Немає доступу до Form events
- ❌ Немає доступу до UI елементів (кнопки, таблиці)
- ❌ Немає Form context

ObjectModule тести підходять для:
- ✅ Бізнес-логіки (обчислення, валідація)
- ✅ Exported процедур
- ✅ Швидких unit-тестів

### 4. Структура тестового файлу

```bsl
// tests/form_tests.bsl

#Область Тести_Арифметики

&НаСервере
Процедура Тест_Додавання() Экспорт
    // Тест логіка
КонецПроцедуры

#КонецОбласті

#Область Тести_УправлінняСтаном

&НаСервере
Процедура Тест_Очищення() Экспорт
    // Тест логіка
КонецПроцедуры

#КонецОбласті
```

## Міграція з v2.16.0 → v2.23.2

Якщо у вас є старі тести у форматі v2.16.0:

### Було (v2.16.0):
```yaml
connection_string: "File=\"{temp_ib}\";"
epf_file_name: Processor.epf

tests:
  - name: test_addition
    type: procedural
    connection_type: Automation
    procedure: |
      Форма = ПолучитьФорму("...");
      Форма.Объект.Число1 = 5;
```

### Стало (v2.23.2):
```yaml
# tests/tests.yaml
forms:
  - name: Форма
    procedural:
      file: form_tests.bsl
      procedures: [Тест_Додавання]
```

```bsl
// tests/form_tests.bsl
&НаСервере
Процедура Тест_Додавання() Экспорт
    Объект.Число1 = 5;  // Без ПолучитьФорму - ми вже в контексті форми
    ...
КонецПроцедуры
```

## Додаткова інформація

- **Документація:** `docs/LLM_TESTING_WORKFLOW.md` - покроковий workflow
- **Робочий приклад:** `examples/yaml/calculator_with_tests/` - повний приклад з декларативними + процедурними тестами
- **API Reference:** `docs/TESTING_AUTOMATION.md` - детальна документація по API

## Troubleshooting

### "Total tests: 0"
**Причина:** Використовується старий формат `tests:` замість `forms:`

**Рішення:** Використайте новий формат v2.23.2 (див. вище)

### "Cannot import AutomationServerConnection"
**Причина:** Помилка імпорту в test_runner.py (вже виправлена)

**Рішення:** Переконайтесь що використовуєте останню версію генератора

### Тести не знаходять процедури
**Причина:** BSL файл не у правильному місці або неправильний шлях в tests.yaml

**Рішення:** Шлях `file:` має бути відносним від `tests.yaml`
